if (MU_BUILD_TYPE STREQUAL "cross")
    message(STATUS "Cross-compiling LibGeneric for kernel build")
    include(Platform/MuOS/CMakeLists.txt)
elseif (MU_BUILD_TYPE STREQUAL "test")
    message(STATUS "Compiling LibGeneric for native platform")
    project(LibGeneric)
    cmake_minimum_required(VERSION 3.15)
    include_directories(../)
    include(Platform/Native/CMakeLists.txt)
else ()
    message(FATAL_ERROR "Unknown build type '${MU_BUILD_TYPE}'!")
endif ()
